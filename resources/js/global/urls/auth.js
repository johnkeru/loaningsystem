const TEST = "api/test";

const LOGIN = "api/login";
const REGISTER = "api/register";
const LOGOUT = "api/logout";

const ADD_MONEY = 'api/add_money'

export default {
    TEST,
    LOGIN,
    REGISTER,
    LOGOUT,

    ADD_MONEY,
};
